# Hardware registry entitlement (spike)

JSON Schema: [`entitlement.schema.json`](entitlement.schema.json).

Example signed for the empty-parts fingerprint (SHA-256 of empty string) with
dev secret `example-dev-secret`:

```text
EARNIE_REGISTRY_DEV_SECRET=example-dev-secret
```

See [`docs/spec/hardware-registry-layer-c.md`](../../docs/spec/hardware-registry-layer-c.md).
