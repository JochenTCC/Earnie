{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://earnie.local/loxone/recipes/recipe.schema.json",
  "title": "Loxone Merker / Baustein Recipe (2.4.g)",
  "description": "JSON Merker recipes (2.4.n greenfield contract). suggested_name must match share/loxone/greenfield_device_map.json. VI/VO XML drafts live under share/loxone/templates/. See docs/referenz/loxone-signale.md.",
  "type": "object",
  "required": ["role_id", "label", "recommended_markers"],
  "properties": {
    "role_id": {
      "type": "string",
      "enum": ["grid", "pv", "ess", "evcs", "consumer", "heatpump", "pool"]
    },
    "label": {
      "type": "string",
      "minLength": 1
    },
    "baustein_notes": {
      "type": "string",
      "description": "Text tips for Virtual Status / Memory / Marker layout — not importable Config."
    },
    "recommended_markers": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["suggested_name"],
        "properties": {
          "ehal_field": {
            "type": "string",
            "description": "M1 EHAL field when applicable."
          },
          "loxone_extra": {
            "type": "string",
            "description": "Non-EHAL extra key when ehal_field is absent (rare; prefer ehal_field)."
          },
          "loxone_blocks_key": {
            "type": "string",
            "minLength": 1,
            "description": "Legacy / thermal / filter config path hint (optional for stub pool markers)."
          },
          "suggested_name": {
            "type": "string",
            "minLength": 1
          },
          "notes": {
            "type": "string"
          }
        },
        "additionalProperties": false,
        "anyOf": [
          { "required": ["ehal_field"] },
          { "required": ["loxone_extra"] }
        ]
      }
    },
    "reference": {
      "type": "string",
      "description": "Doc path for signal reference."
    }
  },
  "additionalProperties": false
}
