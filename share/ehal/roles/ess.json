{
  "role_id": "ess",
  "label": "Batterie (ESS)",
  "description": "Home battery SoC, optional power, charge/discharge limits, and optional mode.",
  "status": "m1",
  "ehal_fields": [
    {
      "field": "sens_ess_soc",
      "required": true,
      "kind": "telemetry",
      "notes": "SoC in % (0…100)."
    },
    {
      "field": "sens_ess_power",
      "required": false,
      "kind": "telemetry",
      "notes": "Optional W; + = discharge, − = charge (OpenEMS-aligned)."
    },
    {
      "field": "set_ess_charge_power_limit",
      "required": false,
      "kind": "setpoint",
      "notes": "Max charge power magnitude in W."
    },
    {
      "field": "set_ess_discharge_power_limit",
      "required": false,
      "kind": "setpoint",
      "notes": "Max discharge power magnitude in W."
    },
    {
      "field": "set_ess_mode",
      "required": false,
      "kind": "setpoint",
      "notes": "ESS mode / control command (e.g. Huawei); replaces target_soc write."
    },
    {
      "field": "supports_ess_write",
      "required": true,
      "kind": "capability",
      "notes": "True when charge/discharge limit writes are available."
    }
  ],
  "capability_expectations": {
    "supports_ess_write": true
  },
  "unit_sign_notes": "See docs/spec/ehal.md § Units and sign convention."
}
