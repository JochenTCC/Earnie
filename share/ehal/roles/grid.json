{
  "role_id": "grid",
  "label": "Netz (PCC)",
  "description": "Point of common coupling — grid import/export active power.",
  "status": "m1",
  "ehal_fields": [
    {
      "field": "grid_power_active",
      "required": true,
      "kind": "telemetry",
      "notes": "W; + = import (Bezug), − = export (Einspeisung)."
    }
  ],
  "unit_sign_notes": "See docs/spec/ehal.md § Units and sign convention."
}
