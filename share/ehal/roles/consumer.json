{
  "role_id": "consumer",
  "label": "Flex-Verbraucher (Stub)",
  "description": "Forward-looking template. M1 EHAL has no first-class consumer fields; Live still uses house-profile loxone_inputs/outputs and flex markers.",
  "status": "stub",
  "ehal_fields": [
    {
      "field": "flex.{hk_id}.sens_power_act",
      "required": false,
      "kind": "stub",
      "notes": "Pattern B power. Legacy binding key flex.power_name migrates on load. Zähler id zaehler_<slug> uses slug without prefix."
    },
    {
      "field": "flex.{hk_id}.set_enable",
      "required": false,
      "kind": "stub",
      "notes": "Pattern B Freigabe. Legacy flex.enable_name migrates on load."
    },
    {
      "field": "flex.{hk_id}.set_power_setpoint",
      "required": false,
      "kind": "stub",
      "notes": "Pattern B Sollwert. Legacy flex.power_setpoint_name migrates on load."
    }
  ],
  "unit_sign_notes": "Stub only — not part of M1 wire. See docs/referenz/loxone-signale.md § Flexible Verbraucher."
}
