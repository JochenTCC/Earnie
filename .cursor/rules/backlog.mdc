---
description: Backlog.md pflegen — erledigte Punkte verschieben, nicht durchstreichen
globs: Backlog.md,Backlog-Erledigt.md
alwaysApply: false
---

# Backlog.md & Backlog-Erledigt.md

**Offen:** `Backlog.md` (Bugfixes, Feature-Backlog nach Version, Packaging, Referenz)

**Archiv:** `Backlog-Erledigt.md` (abgeschlossene Arbeiten)

Beim Pflegen:

- **Erledigte Punkte nicht durchstreichen** (`~~…~~` vermeiden).
- Stattdessen aus den offenen Kapiteln in `Backlog.md` entfernen und in `Backlog-Erledigt.md` eintragen:
  - Neuer Abschnitt `### <Thema> (YYYY-MM-DD)` (Datum Europe/Vienna)
  - Einzelpunkte mit `- [x] …`
- Bei **teilweise erledigten** Epics: nur die offenen Phasen/Unterpunkte in `Backlog.md` belassen.
- Nur dokumentieren, was tatsächlich erledigt wurde — nichts erfinden.
- Referenz-Tabellen (z. B. Log-Dateien) gehören nicht ins Erledigt-Archiv, sondern unter **Referenz** in `Backlog.md`.
- Wiederholte `### Version …`-Überschriften in `Backlog.md` sind bewusst — Reihenfolge per Block verschieben, nicht zusammenführen.
